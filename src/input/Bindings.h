// =============================================================================
// src/input/Bindings.h — P1/P2 기본 액션 매핑 헬퍼.
//
// 목적:
//   - 메인 플랜 §4.3 의 표준 키 매핑을 한 줄 호출로 InputManager 에 주입.
//
// P1: WASD + 1/2/3 (무기) + F (발사) + G (박격포 각도 키).
// P2: 화살표 + 키패드 1/2/3/0/Enter.
//
// scancode 사용 ([5. Input Handling.pdf p.7]) — 키보드 레이아웃 무관.
//
// 출처: 메인 플랜 §4.3 Action Map. 강의 PDF 자체에 본 키 매핑이 명시되어
// 있지 않음 (게임별 결정 사항).
// =============================================================================

#pragma once

class InputManager;

// 모든 기본 액션을 등록. 호출자는 InputManager 인스턴스를 보유.
//   - 본 함수는 멱등 (idempotent) 가 아님. 두 번 호출하면 같은 binding 이
//     중복 등록되어 IsPressed 가 두 번 검사할 뿐 큰 문제는 없지만 이중 호출
//     하지 않는 것이 깔끔.
void BindDefaultActions(InputManager& im);
