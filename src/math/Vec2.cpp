// =============================================================================
// src/math/Vec2.cpp — Vec2 의 비-인라인 본문.
//
// 본 파일은 의도적으로 비어 있다 (Vec2 의 모든 메서드는 헤더에 inline 정의).
// CMake 의 add_executable 이 .cpp 가 하나라도 있어야 안정적으로 처리되는 경우
// 가 있어 빈 .cpp 를 둔다. 향후 Length() 가 inline 에서 빠지거나, 별도
// implementation 이 필요해지면 (예: 디버그 출력) 본 파일에 작성한다.
//
// Plan Revisability 노트: Week 9 PDF 가 벡터 보간 (Lerp / Slerp) 같은 보조
// 함수를 추가로 다루면 그 함수들의 본문을 여기에 둘 수 있다.
// =============================================================================

#include "Vec2.h"
